# SmartPipe.Core 2.2.0 — Comparative Performance & Stress Lab

**Status:** experimental plan on `perf/2.2.0-comparative-lab`  
**Prepared:** 2026-09-21  
**Purpose:** obtain reproducible evidence for the full SmartPipe 2.2.0 architecture through SP220-12 against the published 2.1.2 baseline.

## 0. Repository truth at lab creation

The comparison must be anchored to immutable revisions, not moving branch names.

| Role | Ref / SHA | State |
|---|---|---|
| Published baseline | `8e79902d22de714f493582946f7c260462b0895e` | SmartPipe 2.1.2 |
| `release/2.2.0` | `0439163f43391f219a70a9008e7dafa591d4ae35` | currently only through Checkpoint D |
| Checkpoint E integration branch | `61ceef6bf69aef0a4f79b25384352d238979200f` | SP220-01…SP220-11 integrated |
| Full SP220-01…SP220-12 candidate | **UNPINNED** | must be pinned only after SP220-12 is accepted into `sp220/checkpoint-e` |

The lab branch is created now from the most advanced accepted integration head (`61ceef6b...`) so the harness can be designed independently. **No performance claim named “2.1.2 vs full 2.2.0 SP220-01…12” is valid until SP220-12 is merged and its exact SHA is frozen in the lab manifest.**

The product under test is never identified by the lab branch HEAD. The lab branch contains the experiment harness. Product targets are immutable SHAs/package hashes.

---

# 1. Research question

The lab must answer five separate questions instead of collapsing everything into one “faster/slower” number:

1. **Core efficiency:** did equivalent pipeline workloads become faster or cheaper in allocations/GC?
2. **Scalability:** did throughput, backpressure behaviour and concurrent-run capacity improve or regress?
3. **Lifecycle robustness under load:** do start/drain/abort/dispose/cancellation/failure paths remain bounded and leak-free?
4. **Integration efficiency:** what did SP220-03…12 change for DI, Hosting, HealthChecks, telemetry, Channels/Transforms/Logging/DataAnnotations, JSON, CSV, Dapper, EF Core and Mapster?
5. **Trade-offs:** where 2.2.0 intentionally pays startup/configuration cost for stronger isolation, ownership, safety, package boundaries or predictable cleanup, is the cost measured and justified?

A “win” is not defined only by lower mean latency. The report must show throughput, latency distribution where meaningful, allocation rate, GC, memory retention, ThreadPool behaviour, correctness and resource lifetime.

---

# 2. Experimental classes

Every scenario is classified before implementation.

## A. Strict A/B comparable

Same externally observable workload can be expressed against both 2.1.2 and 2.2.0 without changing semantics.

Examples:
- Core source → transform(s) → sink throughput;
- bounded channel/backpressure behaviour;
- legacy-compatible JSON/CSV paths;
- legacy Dapper selector / DB sink equivalents;
- legacy EF selector equivalents;
- legacy Mapster transform equivalent once SP220-12 exists;
- logging disabled/enabled paths where semantics match.

These scenarios may report:
- 2.2.0 / 2.1.2 ratio;
- percentage change;
- allocation delta;
- statistically supported regression/improvement.

## B. Semantically comparable, API-different

The workload goal is the same but the canonical 2.2.0 API is different.

Examples:
- 2.1.2 immediate builder/start versus 2.2.0 reusable immutable definition;
- legacy connection/context ownership versus run-owned factory model;
- old broad integration versus 2.2.0 leaf package canonical path.

These may report side-by-side numbers, but the report must explicitly label them **evolution comparisons**, not pure micro-regressions.

## C. 2.2.0-only characterization

No honest 2.1.2 equivalent exists.

Examples:
- cached generic definition compilation;
- reusable concurrent definitions;
- keyed DI registry/provider;
- multi-run keyed health observation;
- strict CSV bounded framing;
- Mapster isolated private configuration template;
- package-specific AOT/trim behaviour.

These receive absolute measurements and scaling curves only. No invented “% faster than 2.1.2”.

---

# 3. Lab architecture

Target layout:

```text
perf/
  README.md
  manifests/
    targets.json
    scenarios.json
    thresholds.json
  src/
    SmartPipe.Perf.Shared/
    SmartPipe.Perf.Benchmarks/
    SmartPipe.Perf.Stress/
    SmartPipe.Perf.Soak/
    SmartPipe.Perf.Target.V212/
    SmartPipe.Perf.Target.V220/
    SmartPipe.Perf.Reporting/
  fixtures/
    json/
    csv/
    sqlite/
  artifacts/                 # ignored
  results/                   # ignored raw local output
  reports/                   # committed curated summaries only

eng/
  SmartPipe.PerfLab/
    prepare-target
    verify-environment
    run-benchmarks
    run-stress
    run-soak
    compare
    report

.github/workflows/
  perf-lab.yml
```

## 3.1 One harness, two product targets

Do **not** keep a 2.1.2 benchmark project in the 2.1.2 tree and a different benchmark project in the 2.2.0 tree.

Instead:

```text
published 2.1.2 packages ─┐
                          ├─ same workload definitions
packed exact 2.2.0 SHA ───┘
                          │
                          ▼
                  same lab runner/config
```

The target adapters isolate API differences while shared workload definitions, data generation, iteration counts, result schema and correctness checks stay common.

Two SmartPipe versions must not be loaded side-by-side into one process because they use the same assembly identities. The harness builds/runs isolated executables for each target.

## 3.2 Target materialization

### 2.1.2

Use the existing immutable baseline:
- `eng/baselines/2.1.2/manifest.json`;
- published NuGet packages;
- existing SHA-256/signature verification.

Never rebuild the baseline silently from moving source and call it “2.1.2”.

### 2.2.0

After SP220-12 acceptance:
1. pin exact Checkpoint-E SHA;
2. clean checkout;
3. locked restore;
4. Release build with warnings as errors;
5. existing repository gates;
6. `pack-packages` from the package graph;
7. record every package SHA-256;
8. use this immutable local feed in all lab runs.

The target manifest records both Git SHA and package hashes.

---

# 4. Environment contract

Primary comparisons are valid only when both targets run under the same environment contract.

Record for every run:

- OS + version/build;
- architecture;
- CPU model, logical/physical core count;
- RAM;
- .NET SDK and runtime versions;
- BenchmarkDotNet version;
- power profile;
- virtualization/container state;
- GC mode;
- processor affinity if used;
- repository/harness SHA;
- baseline and candidate SHAs;
- package hashes;
- process environment relevant to .NET/JIT/GC;
- UTC timestamp;
- run order.

The current repository pins SDK `10.0.303`; the lab should keep the runner/toolchain fixed for the primary comparison unless an explicit toolchain experiment says otherwise.

## 4.1 Run-order bias

Do not run all baseline measurements first and candidate measurements hours later.

Preferred full-run order:

```text
A1 → B1 → B2 → A2
A3 → B3 → B4 → A4
```

or another fixed counter-balanced schedule.

This reduces bias from thermal state, background activity and machine drift.

## 4.2 GitHub-hosted runners

GitHub-hosted runners are useful for:
- compilation;
- benchmark discovery;
- BenchmarkDotNet Dry/Short smoke;
- correctness prechecks;
- fixed-work stress correctness;
- leak/lifecycle invariants;
- artifact/schema validation.

They are **not authoritative for hard wall-clock regression gates** because the underlying hardware/noise is not controlled.

Authoritative performance ratios require a controlled reference machine or dedicated self-hosted runner.

---

# 5. Measurement layers

## Layer 1 — BenchmarkDotNet micro/meso

Purpose: isolated CPU/allocation/startup/hot-path measurements.

Required:
- Release;
- separate benchmark processes;
- warmup/pilot handled by BenchmarkDotNet;
- `MemoryDiagnoser`;
- `ThreadingDiagnoser` only on scenarios where it adds signal;
- correctness precheck outside the measured operation;
- raw BenchmarkDotNet JSON/CSV/Markdown artifacts retained;
- no manually added inner loop merely to “make timings larger” unless the benchmark is explicitly a batch operation.

Main statistics:
- mean;
- median;
- error/confidence information from BDN;
- ratio where valid;
- allocated bytes/op;
- Gen0/Gen1/Gen2;
- completed work items / lock contention for selected concurrent scenarios.

## Layer 2 — deterministic stress

Purpose: find saturation, races, unbounded queues, cleanup bugs and concurrency degradation.

Stress is expressed primarily in **work units**, not “sleep for N seconds”:
- exact item counts;
- exact concurrent run counts;
- deterministic failure schedule;
- deterministic cancellation point;
- deterministic seeds where randomized chunking is useful.

Collect:
- completed items;
- failed/filtered/dropped items;
- total elapsed time;
- items/s;
- peak/current queue depth where available;
- allocation/GC counters;
- working set;
- ThreadPool thread count and queue length;
- exceptions;
- SmartPipe metrics required to prove workload equivalence.

## Layer 3 — soak/leak

Purpose: detect retention and long-lived degradation that microbenchmarks cannot see.

Profiles:
- 30 min developer smoke;
- 60 min standard lab;
- 120 min extended/nightly.

Check:
- managed heap trend after warmup;
- working-set trend;
- Gen2 frequency;
- thread count;
- ThreadPool queue;
- handle/fd trend;
- active run count;
- connection/context/resource return;
- error/drop/retry counters;
- final full cleanup.

Use `dotnet-gcdump` only at selected checkpoints because collection triggers a full GC and would contaminate the performance interval.

## Layer 4 — process/application scenarios

Optional second stage using Microsoft.Crank for stable application-level orchestration:
- Generic Host startup/run/shutdown;
- telemetry-enabled host;
- later HTTP scenarios.

Crank is not required for the first implementation because SmartPipe is primarily an in-process library; BenchmarkDotNet + deterministic stress are the primary tools.

---

# 6. Scenario matrix for SP220-01…12

## SP220-01 — package/infrastructure

Mostly non-runtime. Measure separately from hot-path claims:

- restore/package closure size;
- package count/direct dependency closure;
- build/pack time as engineering metrics;
- cold application load/start footprint for direct leaf consumer versus broad facade;
- loaded assembly count for representative minimal consumers.

Do not claim package split makes the pipeline runtime faster unless runtime measurements prove it.

## SP220-02 — Core definition model

Benchmark:
- build definition: 0 / 1 / 10 stages;
- first compile: 0 / 1 / 10 stages;
- cached compile;
- first StartAsync;
- repeated StartAsync on reusable definition;
- single-item and 1k/100k/1M item flow;
- stage counts 0 / 1 / 5 / 10;
- allocation/item;
- run creation/disposal cost.

Stress:
- 32 reusable-definition concurrent runs;
- 1000 sequential runs;
- concurrent dispose;
- activation failure + rollback;
- cancellation at source/stage/sink boundaries.

Compare legacy-compatible workload against 2.1.2 separately from new-definition characterization.

## SP220-03 — Dependency Injection

Benchmark:
- AddSmartPipe registration;
- AddPipeline registration;
- keyed factory lookup;
- StartAsync scope creation/readiness;
- terminal unregister + scope disposal.

Stress:
- many keys;
- same generic type pair with many keys;
- 32 parallel runs;
- scoped component creation/disposal;
- start failure cleanup.

Metrics: startup overhead and allocations; no artificial service-resolution loops that do not represent real runs.

## SP220-04 — Hosting

Meso/process scenarios:
- 1 / 8 / 32 hosted pipelines;
- sequential startup;
- reverse stop;
- drain success;
- drain timeout → abort;
- partial-start rollback;
- pipeline fault monitoring.

Primary outputs:
- host-to-ready latency;
- stop latency;
- cleanup completion;
- leaked runs/scopes = 0.

No hard timing gate on GitHub-hosted runners.

## SP220-05 — HealthChecks

Benchmark:
- liveness evaluation;
- readiness evaluation;
- aggregate readiness over 1 / 8 / 32 keys;
- observation snapshot publication.

Stress:
- concurrent run updates + health reads;
- 32 active runs/key where supported;
- terminal snapshot replacement.

Report lookup/evaluation allocations and contention separately from Core hot path.

## SP220-06 — OpenTelemetry

Four conditions:
1. instrumentation package not registered;
2. registered, no exporter/listener consuming;
3. in-memory metrics/traces enabled;
4. representative OTLP exporter setup in process scenario.

Measure:
- overhead/item;
- allocations/item;
- activity creation rate;
- metric recording cost;
- effect on throughput.

The lab must not blame SmartPipe for exporter/network cost without separating exporter-enabled results.

## SP220-07 — Channels / Transforms / Logging / DataAnnotations

Channels:
- 1/2/N readers;
- bounded capacities 1 / 32 / 1024;
- fast producer/consumer;
- producer faster than consumer;
- cancellation while output is blocked.

Transforms:
- conditional/filter/composite;
- 1/5/10 transform chains;
- allocation/item.

Logging:
- log disabled;
- enabled with no payload formatter;
- formatter path;
- bounded payload formatting.

DataAnnotations:
- legacy reflection path;
- AOT-safe rule path;
- valid/invalid mix.

## SP220-08 — JSON

Benchmark:
- JSON array streaming;
- NDJSON;
- 1 / 1k / 100k records;
- source-generated metadata path;
- sink;
- dead-letter framing where comparable.

Stress:
- random deterministic chunk sizes;
- split UTF-8;
- oversized record handling;
- early break;
- cancellation while framing;
- 32 concurrent runs.

Use memory-backed streams for parser throughput. File-backed scenarios are separate and not hard cross-OS latency gates.

## SP220-09 — CSV

Required matrix from the design is preserved:
- legacy source 100k;
- strict source 100k;
- quoted multiline;
- oversized skip path;
- legacy sink 100k;
- strict sink 100k;
- allocation/record;
- definition compile/start separate from I/O.

Additional stress:
- 32 concurrent sources/sinks;
- random deterministic chunking;
- cancellation in framing states;
- cleanup/rollback failure paths.

## SP220-10 — Dapper

Use SQLite/in-process provider for reproducible local integration and provider fakes for lifecycle paths.

Benchmark:
- source 1 / 1k / 100k rows;
- default mapper;
- explicit mapper;
- sink batch=1;
- sink batch=100;
- transaction none/per-batch;
- definition/start overhead.

Stress:
- 100k streaming rows with bounded memory;
- 100k writes;
- 32 concurrent definitions;
- cancellation/early break;
- transaction rollback and cleanup.

Provider/network latency is informational. Hard gates focus on allocation shape, boundedness and lifecycle.

## SP220-11 — Entity Framework Core

Benchmark:
- 1 / 100 / 10k rows;
- NoTracking / identity-resolution / Tracking;
- pooled vs regular context factory;
- queryable vs compiled query;
- early break at 1 / 100.

Stress:
- 1000 sequential runs;
- 32 concurrent runs × 100 iterations;
- distinct context identity;
- no cross-run query reuse;
- context/entity collection checks.

Document provider buffering separately; do not claim provider-wide constant memory.

## SP220-12 — Mapster

This matrix is planned from the SP220-12 specification and becomes executable only after the implementation SHA exists.

Benchmark:
- configuration/template creation;
- cold compile;
- first item after InitializeAsync;
- steady-state flat mapping;
- nested/collection mapping;
- 100k mappings;
- allocations.

Stress:
- 32-run isolation;
- no GlobalSettings coupling;
- no lazy compile leaking into first steady-state item;
- deterministic behaviour under test-order repetition.

Compare the legacy `MapsterTransform<TInput,TOutput>` path against 2.1.2 only where semantics match. Treat the isolated canonical path as a 2.2.0 evolution/characterization scenario.

---

# 7. Backpressure and saturation profiles

Core stress profiles:

| Profile | Producers | Consumers | Capacity | Goal |
|---|---:|---:|---:|---|
| SPSC-small | 1 | 1 | 1 | maximum backpressure pressure |
| SPSC-normal | 1 | 1 | 256 | steady throughput |
| MPSC | 8 | 1 | 256 | contention |
| MPMC | 8 | 8 | 1024 | scheduler/coordination |
| Slow sink | fast | 1 | 32 | bounded waiting |
| Burst | burst 100k | variable | 256 | queue recovery |

For slow-consumer correctness use gates/channels where possible. Do not make correctness depend on fragile `Task.Delay` timing.

---

# 8. Failure profiles

Failures must be deterministic:

- every Nth item fails;
- fail first K attempts then succeed;
- always fail;
- cancel after N accepted items;
- cancel while producer is blocked;
- cancel while sink is blocked;
- dispose concurrently from N callers;
- fault during initialization;
- fault during operation plus fault during cleanup.

Validate both outcome and resource counts before using timings.

---

# 9. Diagnostics collection

## Always-on run metadata

- environment manifest;
- BDN raw result;
- stdout/stderr;
- target package manifest;
- SmartPipe counters/snapshot summary.

## Stress/soak counters

Collect with `dotnet-counters`:
- `System.Runtime`;
- SmartPipe Meter where relevant.

Focus on:
- CPU;
- working set;
- GC heap;
- allocation rate;
- Gen collections;
- ThreadPool thread count;
- ThreadPool queue length;
- exceptions.

## Escalation

When a regression is observed:
1. reproduce;
2. capture `dotnet-trace`;
3. inspect waits/CPU/GC;
4. use `dotnet-gcdump` only for suspected retention;
5. add a minimized benchmark/reproducer before optimizing.

---

# 10. Result model

Raw result schema must separate measurement from interpretation.

Each record carries at least:

```text
scenario_id
scenario_class = strict-ab | evolution | v220-only
baseline_sha
candidate_sha
harness_sha
environment_id
run_id
run_order
metric
unit
value
sample_count
artifact_path
correctness_passed
```

Generated report should contain:

| Scenario | Class | 2.1.2 | 2.2.0 | Ratio/Δ | Alloc Δ | Evidence |
|---|---|---:|---:|---:|---:|---|

For v220-only scenarios, baseline/ratio stays blank.

---

# 11. Regression policy

## Phase 1 — evidence only

Until the lab has enough history:
- no hard wall-clock PR failure threshold;
- flag changes for review;
- allocations/boundedness/lifecycle invariants may be hard gates immediately;
- exact correctness mismatch is always failure.

## Phase 2 — controlled-runner gates

After at least several stable reference runs:
- per-scenario thresholds, not one global “10%” rule;
- practical significance floor;
- repeated/counter-balanced A/B evidence;
- regression requires reproducibility before release blocking.

Example policy classes:
- Core hot path: tight threshold;
- integration CPU/allocation: moderate threshold;
- disk/provider/network latency: informational;
- startup: evaluate separately from steady state;
- soak: slope/leak invariant rather than raw latency.

Never optimize to one aggregate score.

---

# 12. CI design

The lab uses `.github/workflows/perf-lab.yml` as an isolated CI lane instead of adding expensive work to normal release validation.

## 12.1 September 2026 GitHub Actions constraints

Repository state matters to CI economics:

- `MrFr3di/SmartPipe-Core` is public;
- standard GitHub-hosted runners are free for public repositories and do not consume the private-repository monthly minutes allowance;
- larger runners remain billable even for public repositories and are therefore excluded from the lab;
- the default Actions cache allowance is 10 GB per repository;
- cache entries not accessed for more than 7 days are automatically removed, and least-recently-used entries are evicted when storage pressure requires it;
- artifact storage remains finite and must not be treated as a permanent performance database.

Therefore the lab optimizes for **feedback latency, cache/storage pressure and evidence quality**, not merely runner minutes.

## 12.2 Trigger topology

### Push to the lab branch

Only paths that can change the experiment trigger the lab smoke lane:

- `.github/workflows/perf-lab.yml`;
- `perf/**`;
- `eng/perf/**`;
- `docs/performance/**`;
- benchmark project files;
- SDK/package dependency files and lock files.

A documentation or source change elsewhere in the repository does not spend a lab run.

### Manual dispatch

`workflow_dispatch` owns expensive modes:

- `smoke`;
- `benchmark-smoke`;
- `stress`;
- `soak`.

Until PERF-08/PERF-09 exist, `stress` and `soak` are fail-closed placeholders. CI must never substitute an ad-hoc timing loop merely because the final runner is unfinished.

No automatic full benchmark is scheduled during bootstrap. After the harness is mature, a low-frequency scheduled **informational** run may be enabled on the default/protected integration branch. It must not become a hard performance gate on GitHub-hosted hardware.

## 12.3 Concurrency policy

The lab uses one workflow-level concurrency group per branch.

Superseded push/smoke runs are canceled. Long explicit runs must not be silently replaced once authoritative controlled-run execution exists.

This prevents a sequence of small plan/harness commits from leaving several obsolete benchmark jobs queued.

## 12.4 Runner policy

Use standard `ubuntu-latest` for:

- manifest/schema validation;
- restore/build;
- BenchmarkDotNet Dry/Short smoke;
- deterministic correctness/stress smoke;
- report generation.

Do not use paid larger runners.

GitHub-hosted timing is informational because VM placement and host noise are outside our control. For an A/B informational run, **baseline and candidate execute in the same job on the same VM**, in counter-balanced order. A matrix with baseline and candidate on different runner VMs is forbidden for performance comparison.

Authoritative release performance evidence is produced on a controlled machine by the same harness. A permanent self-hosted GitHub Actions runner is not planned for this public repository: untrusted public-PR code makes persistent self-hosted runners a security risk. If automation is needed later, prefer an isolated disposable/JIT environment or run the harness outside Actions and attach only sanitized evidence.

## 12.5 NuGet cache policy

Use `actions/setup-dotnet` built-in caching instead of a second `actions/cache` layer:

```yaml
env:
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

- uses: actions/setup-dotnet@<pinned-sha>
  with:
    global-json-file: global.json
    cache: true
    cache-dependency-path: '**/packages.lock.json'
```

Reasons:

- the repository already uses lock files and locked restore;
- `setup-dotnet` caches the NuGet global-packages directory;
- dependency changes produce a different cache key;
- a duplicate manual NuGet cache would consume additional cache entries without adding useful isolation.

Allowed in cache:
- immutable third-party NuGet package downloads.

Forbidden in cache:
- `bin/`, `obj/`;
- local candidate `.nupkg`/`.snupkg`;
- materialized 2.1.2 baseline packages as experimental evidence;
- BenchmarkDotNet artifacts;
- stress/soak results;
- traces, gcdumps, coverage or generated reports;
- any token, NuGet credential or secret.

Product artifacts are provenance-bearing inputs and must be rebuilt/provisioned and hash-verified for each exact target definition, not restored from an opaque cache.

During PERF-02, narrow `cache-dependency-path` from the repository-wide glob to the PerfLab/adapter lock files if measurement shows this materially reduces cache churn without missing dependencies.

## 12.6 Cache security

Cache is an optimization, not a trust boundary.

- Actions remain pinned by immutable commit SHA.
- Workflow permissions remain `contents: read`.
- `persist-credentials: false` is required on checkout.
- No `pull_request_target` execution of untrusted benchmark code.
- No secrets are written to the NuGet cache.
- A cache hit never substitutes package-hash/baseline verification.

## 12.7 Artifact/storage policy

Raw output retention is deliberately short:

| Artifact | Retention | Compression |
|---|---:|---|
| push smoke | no upload by default | n/a |
| manually requested BDN smoke | 3 days | high for text/JSON/CSV |
| full BDN raw results | 7 days | high |
| stress machine-readable results | 7 days | high |
| large trace/gcdump diagnostic | 1–3 days | low/none when already compressed |
| curated final report | committed/reviewed | n/a |

Do not upload complete `bin/obj`, NuGet global cache or duplicated package feeds.

A curated comparison report is the durable artifact. Raw transient evidence is reproducible from target SHA + package hashes + harness SHA + environment manifest.

## 12.8 Workflow phases

Target topology after PERF-10:

```text
contract/preflight
      ↓
materialize exact A + B
      ↓
correctness oracle
      ↓
same runner / same job
A1 → B1 → B2 → A2
      ↓
compare + classify
      ↓
short-lived raw artifact
      ↓
curated report
```

Stress and soak use the same target materialization contract but different runners/workloads.

## 12.9 Current bootstrap implementation

Already present on the lab branch:

- `perf/manifests/targets.json`;
- `perf/manifests/scenarios.json`;
- `eng/perf/validate-perf-lab.ps1`;
- `.github/workflows/perf-lab.yml`;
- path-scoped push smoke;
- `setup-dotnet` NuGet cache;
- Release benchmark build;
- BenchmarkDotNet `Dry` run for definition benchmarks;
- three-day artifact retention only for manually requested benchmark smoke;
- explicit blocking of unfinished heavy modes.

The existing `benchmarks/SmartPipe.Benchmarks` project remains a **product-local smoke source only**. It is not the final comparative harness because it references current source projects directly. PERF-02 replaces comparative execution with isolated 2.1.2/2.2.0 target adapters consuming immutable package feeds.

---

# 13. Implementation sequence

## PERF-00 — Freeze methodology

- create manifests/schema;
- define A/B/evolution/v220-only classification;
- define environment fingerprint;
- pin baseline 2.1.2;
- add provisional candidate entry marked unpinned;
- acceptance: no command can produce a “full 2.2.0” comparison while SP220-12 SHA is absent.

## PERF-01 — Target materializer

- verify/provision 2.1.2 published artifacts;
- build/pack arbitrary exact candidate SHA;
- verify package hashes;
- create isolated local NuGet feeds;
- record target manifest.

## PERF-02 — Shared workload and target adapters

- common workload contracts;
- v2.1.2 adapter;
- v2.2.0 adapter;
- correctness oracle;
- prove both adapters execute equivalent Core workload.

## PERF-03 — Core/definition benchmarks

- SP220-02 matrix;
- legacy strict A/B;
- canonical 2.2.0 characterization;
- BDN JSON artifacts and comparer.

## PERF-04 — DI/Hosting/Health/OTel

- startup/lifecycle/observation/telemetry scenarios;
- process-level harness where needed;
- no exporter/network mixing with Core numbers.

## PERF-05 — Channels/Transforms/Logging/DataAnnotations

- throughput/contention/allocation matrix;
- bounded backpressure stress.

## PERF-06 — JSON/CSV

- memory-stream throughput;
- file-backed correctness/stress;
- limits/chunking/cancellation.

## PERF-07 — Dapper/EF Core/Mapster

- SQLite/fake-provider controlled datasets;
- mapping/context/transaction matrices;
- SP220-12 activated only after exact accepted SHA exists.

## PERF-08 — Stress runner

- fixed-work profiles;
- counters;
- deterministic failure/cancellation;
- 32-run and 1000-run suites.

## PERF-09 — Soak/leak

- 30/60/120 min profiles;
- counter snapshots;
- optional trace/gcdump escalation;
- trend analysis.

## PERF-10 — CI/reporting

- smoke workflow;
- dedicated-runner full workflow;
- artifact retention;
- markdown + machine-readable comparison report.

## PERF-11 — First frozen comparison

Only after SP220-12:
- update `targets.json` with exact full candidate SHA;
- re-verify both targets;
- perform counter-balanced full run;
- rerun suspicious deltas;
- publish the first `2.1.2-vs-2.2.0-sp220-01-12.md` report.

---

# 14. Acceptance criteria for the lab

The lab is accepted when:

1. baseline package hashes are verified;
2. full SP220-01…12 candidate SHA is immutable and recorded;
3. same harness SHA is used for both targets;
4. every strict A/B scenario has a correctness oracle;
5. API-different comparisons are labeled evolution rather than strict regression;
6. v220-only scenarios cannot emit a fake 2.1.2 ratio;
7. BenchmarkDotNet raw artifacts are retained;
8. memory/allocation data is collected;
9. stress workloads are deterministic/replayable;
10. 32-run concurrency suite passes;
11. 1000-run lifecycle suite passes where applicable;
12. soak ends with zero active SmartPipe runs/resources;
13. environment fingerprint is embedded in every result set;
14. GitHub-hosted results are not used as authoritative hard wall-clock gates;
15. a suspicious regression can be escalated to trace/gcdump evidence;
16. final report distinguishes speed, allocation, scalability, lifecycle robustness and deliberate trade-offs.

---

# 15. Explicit anti-patterns

Forbidden:

- comparing different benchmark source revisions and calling the delta a product improvement;
- comparing moving branch names without recording SHA;
- using one run;
- changing data shape between A and B;
- changing SDK/runtime between A and B without labeling it a toolchain experiment;
- using Debug builds;
- measuring setup accidentally inside a steady-state benchmark;
- comparing file/database/network latency across uncontrolled hosts as a hard gate;
- treating p95 from tiny sample counts as meaningful;
- hiding failed correctness checks and still publishing timings;
- combining startup and steady-state into one number;
- using GitHub-hosted timing noise as a release blocker;
- forcing every scenario into an A/B ratio when 2.1.2 had no equivalent capability.

---

# 16. Primary external references

- Microsoft .NET diagnostics overview: https://learn.microsoft.com/en-us/dotnet/core/diagnostics/
- Microsoft dotnet-counters: https://learn.microsoft.com/en-us/dotnet/core/diagnostics/dotnet-counters
- Microsoft ThreadPool starvation diagnostics: https://learn.microsoft.com/en-us/dotnet/core/diagnostics/debug-threadpool-starvation
- Microsoft profiling tools / dotnet-trace: https://learn.microsoft.com/en-us/dotnet/core/diagnostics/profilers
- Microsoft dotnet-gcdump: https://learn.microsoft.com/en-us/dotnet/core/diagnostics/dotnet-gcdump
- dotnet/performance: https://github.com/dotnet/performance
- dotnet/performance BenchmarkDotNet workflow: https://github.com/dotnet/performance/blob/main/docs/benchmarking-workflow-dotnet-runtime.md
- BenchmarkDotNet: https://benchmarkdotnet.org/
- BenchmarkDotNet diagnosers: https://benchmarkdotnet.org/articles/configs/diagnosers.html
- Microsoft.Crank: https://github.com/dotnet/crank

---

# 17. Immediate next state

The branch may now implement PERF-00…PERF-10 while SP220-12 is being completed, but the first authoritative full-version run is blocked by one repository-truth condition:

```text
SP220-12 merged into sp220/checkpoint-e
        ↓
exact merge SHA recorded
        ↓
full candidate packages packed + hashed
        ↓
A/B lab unlocked
```

This preserves the purpose of the experiment: measure the complete SP220-01…12 product, not an intermediate branch that merely happens to be named 2.2.0.
